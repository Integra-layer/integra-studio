---
phase: 02-wizard-ux-branding-toggle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/build/SKILL.md
  - skills/deploy/SKILL.md
  - skills/brainstorm/SKILL.md
  - skills/review/SKILL.md
autonomous: true
requirements: [WIZ-01, WIZ-02, WIZ-03]

must_haves:
  truths:
    - "Build skill phase approval questions are selectable with 3 options each"
    - "Build skill Phase 4 reads branding choice from .integra/config.json"
    - "Deploy skill has selectable network confirmation with 3 options"
    - "Brainstorm Step 3 confirmation is selectable with 3 options"
    - "Review post-review question remains selectable (already is -- verify format)"
    - "All converted questions have free-text as last option where applicable"
  artifacts:
    - path: "skills/build/SKILL.md"
      provides: "Selectable phase approval and branding-aware Phase 4"
      contains: "1. Yes, proceed"
    - path: "skills/deploy/SKILL.md"
      provides: "Selectable network confirmation"
      contains: "1. Yes, deploy"
    - path: "skills/brainstorm/SKILL.md"
      provides: "Selectable interest confirmation at Step 3"
      contains: "1. Yes"
    - path: "skills/review/SKILL.md"
      provides: "Verified selectable post-review question"
      contains: "1. Yes"
  key_links:
    - from: "skills/build/SKILL.md"
      to: ".integra/config.json"
      via: "reads branding choice to determine Phase 4 behavior"
      pattern: "config.json.*branding"
---

<objective>
Audit and convert all remaining technical configuration questions across build, deploy, brainstorm, and review skills to use selectable AskUserQuestion format with numbered options. Add branding-aware behavior to build skill Phase 4.

Purpose: WIZ-01 requires ALL technical config questions to be selectable. WIZ-02 requires free-text as last option. WIZ-03 prohibits open-ended config questions outside discovery. This plan covers the 4 skills not handled by Plan 01.
Output: Updated skills/build/SKILL.md, skills/deploy/SKILL.md, skills/brainstorm/SKILL.md, skills/review/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md
@skills/build/SKILL.md
@skills/deploy/SKILL.md
@skills/brainstorm/SKILL.md
@skills/review/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert build and deploy skill questions to selectable format</name>
  <files>skills/build/SKILL.md, skills/deploy/SKILL.md</files>
  <read_first>
    - skills/build/SKILL.md (identify all approval/decision questions)
    - skills/deploy/SKILL.md (identify all approval/decision questions)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (build skill question audit table, deploy skill question audit table)
  </read_first>
  <action>
**skills/build/SKILL.md changes:**

1. **Phase Execution Protocol Step 2 (Ask Approval)** -- Replace the current text:
   `Ask the user: "Ready to build Phase N? Any changes before I start?" Wait for confirmation.`
   With:
   ```
   Use AskUserQuestion:
   "Ready to build Phase {N}: {phase name}?

   1. Yes, proceed
   2. Show me the detailed plan first
   3. Skip this phase"
   ```

2. **Phase Execution Protocol Step 5 (Ask to Proceed)** -- Replace the current text:
   `Ask: "Phase N complete. Review the output above. Want to proceed to Phase N+1, or iterate on something?"`
   With:
   ```
   Use AskUserQuestion:
   "Phase {N} complete. What next?

   1. Proceed to Phase {N+1}
   2. Make changes to what was built
   3. Review what was built in detail"
   ```

3. **Phase 4: UI Polish** -- Replace the first line:
   `Read docs/FRONTEND.md for design specifications. Read knowledge/design-system.md for Integra design tokens.`
   With:
   ```
   Read `.integra/config.json` for the branding choice. Based on the `branding` field:
   - If `"integra"`: Read `knowledge/design-systems/integra-brand.md` for official Integra design tokens (Coral #FF6D49, Euclid Circular B). Apply the official palette verbatim.
   - If `"custom"`: Read `knowledge/design-systems/custom-brand.md` for the AI-generated design system. If the file has placeholder sections, invoke the ui-ux-pro-max skill to generate the custom design system first.

   Also read docs/FRONTEND.md for page-level design specifications.
   ```

4. **Phase 4: UI Polish** -- Add a new AskUserQuestion after reading the config:
   ```
   Use AskUserQuestion:
   "Applying {branding choice} design system to your UI. Confirm approach?

   1. Yes, apply the design system
   2. Let me review the design tokens first
   3. Switch to the other branding option"
   ```

5. **Completion section Step 3** -- The refinement loop text is already correct (uses AskUserQuestion). Keep as-is.

**skills/deploy/SKILL.md changes:**

1. **After "Network Target" section reads config.json**, add a selectable confirmation:
   ```
   Use AskUserQuestion:
   "Deploying to {network} (Chain ID {chainId}). Confirm?

   1. Yes, deploy to {network}
   2. Switch to {other network}
   3. Cancel deployment"
   ```

2. **Step 5: Deploy Frontend** -- The current text says "Offer the user deployment options: Option A/B/C" but doesn't use AskUserQuestion format. Convert to:
   ```
   Use AskUserQuestion:
   "How should we deploy the frontend?

   1. Docker (recommended for persistent hosting)
   2. Static export (for simple hosting)
   3. Vercel (easiest for demos)
   4. Let me describe a different approach"
   ```

3. **Post-Deployment Refinement** -- Already uses AskUserQuestion correctly. Keep as-is.
  </action>
  <verify>
    <automated>grep -c "1\. Yes, proceed" skills/build/SKILL.md | grep -q "[1-9]" && grep -c "1\. Proceed to Phase" skills/build/SKILL.md | grep -q "[1-9]" && grep -c "config.json.*branding" skills/build/SKILL.md | grep -q "[1-9]" && grep -c "integra-brand.md" skills/build/SKILL.md | grep -q "[1-9]" && grep -c "1\. Yes, deploy" skills/deploy/SKILL.md | grep -q "[1-9]" && grep -c "1\. Docker" skills/deploy/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "1. Yes, proceed" skills/build/SKILL.md returns at least 1 match
    - grep "1. Proceed to Phase" skills/build/SKILL.md returns at least 1 match
    - grep "config.json.*branding" skills/build/SKILL.md returns at least 1 match (or grep "integra/config.json" and "branding" separately)
    - grep "integra-brand.md" skills/build/SKILL.md returns at least 1 match
    - grep "custom-brand.md" skills/build/SKILL.md returns at least 1 match
    - grep "1. Yes, deploy" skills/deploy/SKILL.md returns at least 1 match
    - grep "1. Docker" skills/deploy/SKILL.md returns at least 1 match
    - grep "Let me describe" skills/deploy/SKILL.md returns at least 1 match (free-text option per WIZ-02)
  </acceptance_criteria>
  <done>Build skill has selectable phase approval (3 options), selectable iteration (3 options), branding-aware Phase 4 that reads .integra/config.json and loads correct design system file. Deploy skill has selectable network confirmation (3 options) and selectable frontend deploy method (4 options with free-text last).</done>
</task>

<task type="auto">
  <name>Task 2: Convert brainstorm and review skill questions to selectable format</name>
  <files>skills/brainstorm/SKILL.md, skills/review/SKILL.md</files>
  <read_first>
    - skills/brainstorm/SKILL.md (identify all decision questions -- Step 3, Step 6, Step 7)
    - skills/review/SKILL.md (identify post-review question format)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (brainstorm and review question audit tables)
  </read_first>
  <action>
**skills/brainstorm/SKILL.md changes:**

1. **Step 3 -- Connect to Integra** -- Replace the current AskUserQuestion text:
   `"Does that angle interest you, or should we explore a different direction?"`
   With:
   ```
   Use AskUserQuestion:
   "Does that direction interest you?

   1. Yes, let's shape this into a project
   2. Interesting, but show me a different angle
   3. Not quite -- let me describe what I'm thinking"
   ```

2. **Step 6 -- Optional Research** -- Replace the current text:
   `"Want me to quickly research existing projects similar to this idea? Could surface patterns, pitfalls, or inspiration. (yes / skip)"`
   With:
   ```
   Use AskUserQuestion:
   "Want me to research existing projects similar to this idea? Could surface patterns and pitfalls.

   1. Yes, research before we proceed
   2. Skip -- I'm ready to design"
   ```

3. **Step 7 -- Transition** -- Replace the current text:
   `"Want me to design this? I'll ask a few more questions and then scaffold the whole project. (yes / keep exploring)"`
   With:
   ```
   Use AskUserQuestion:
   "Ready to turn this into a project?

   1. Yes, start the design wizard
   2. Keep exploring other angles
   3. Save this idea for later"
   ```

4. **Steps 1 and 2** -- Keep as open-ended. These are ideation/discovery, not technical config. Per WIZ-03 and WIZ-07, open-ended is correct here.

5. **Step 5 -- Refinement** -- Already uses AskUserQuestion with open-ended text. Keep as-is (refinement loops are always open-ended per CLAUDE.md requirement).

**skills/review/SKILL.md changes:**

1. **Post-Review Step 1** -- The current text already uses AskUserQuestion with selectable options:
   `"Want to fix any of these findings now? (yes — fix criticals first / yes — fix all / no — I'll handle it)"`
   Convert to proper numbered format:
   ```
   Use AskUserQuestion:
   "Want to fix any of these findings now?

   1. Yes, fix critical findings first
   2. Yes, fix all findings
   3. No, I'll handle it myself"
   ```

2. **Post-Review Step 4 -- Refinement loop** -- Already uses AskUserQuestion with open-ended text. Keep as-is.

3. **Step 4: Integra Compliance Review** -- Update the design system reference from:
   `check against knowledge/design-system.md`
   To:
   `check against knowledge/design-systems/integra-brand.md (for Integra branding) or knowledge/design-systems/custom-brand.md (for custom branding) -- read .integra/config.json to determine which`
  </action>
  <verify>
    <automated>grep -c "1\. Yes, let's shape" skills/brainstorm/SKILL.md | grep -q "[1-9]" && grep -c "1\. Yes, research" skills/brainstorm/SKILL.md | grep -q "[1-9]" && grep -c "1\. Yes, start the design" skills/brainstorm/SKILL.md | grep -q "[1-9]" && grep -c "1\. Yes, fix critical" skills/review/SKILL.md | grep -q "[1-9]" && grep -c "design-systems/" skills/review/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "1. Yes, let's shape" skills/brainstorm/SKILL.md returns at least 1 match
    - grep "1. Yes, research" skills/brainstorm/SKILL.md returns at least 1 match
    - grep "1. Yes, start the design" skills/brainstorm/SKILL.md returns at least 1 match
    - grep "Not quite" skills/brainstorm/SKILL.md returns at least 1 match (free-text option per WIZ-02)
    - grep "1. Yes, fix critical" skills/review/SKILL.md returns at least 1 match
    - grep "design-systems/" skills/review/SKILL.md returns at least 1 match
    - Steps 1 and 2 in brainstorm remain open-ended (no numbered options)
    - grep "What's on your mind" skills/brainstorm/SKILL.md returns 1 match (Step 1 preserved as open-ended)
  </acceptance_criteria>
  <done>Brainstorm skill has selectable confirmations at Steps 3, 6, and 7 while keeping Steps 1-2 open-ended. Review skill has properly numbered post-review options and updated design system reference to branding-aware path. All selectable questions have 2-3 numbered options. Free-text escape hatch present on brainstorm Step 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User selection -> skill | User picks from numbered options or provides free-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | .integra/config.json | accept | Build/deploy skills read config for branding/network. Values are enum-constrained (integra/custom, mainnet/testnet). Skills validate by checking against known values. Low risk -- local project config. |
</threat_model>

<verification>
- All 4 skill files have numbered options on their technical/config questions
- No technical config question uses open-ended format
- Open-ended questions (brainstorm Steps 1-2, refinement loops) remain open-ended
- Build Phase 4 references .integra/config.json and both design system files
- Review Integra Compliance references knowledge/design-systems/ path
</verification>

<success_criteria>
- Build skill: selectable phase approval, selectable iteration, branding-aware Phase 4
- Deploy skill: selectable network confirmation, selectable frontend deploy method with free-text last option
- Brainstorm skill: selectable Steps 3/6/7, open-ended Steps 1-2 preserved
- Review skill: properly numbered post-review options, branding-aware compliance check
- All converted questions have 2-4 numbered options per WIZ-01
- Free-text escape hatch present where user might want custom input per WIZ-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ux-branding-toggle/02-02-SUMMARY.md`
</output>
