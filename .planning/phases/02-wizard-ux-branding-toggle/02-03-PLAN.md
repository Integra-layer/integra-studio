---
phase: 02-wizard-ux-branding-toggle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge/design-systems/integra-brand.md
  - knowledge/design-systems/custom-brand.md
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, BRAND-05, BRAND-06]
must_haves:
  truths:
    - "knowledge/design-systems/integra-brand.md exists with Coral #FF6D49 and Euclid Circular B"
    - "knowledge/design-systems/custom-brand.md exists as a template for ui-ux-pro-max"
    - "Both files have identical section headings for consistent output format"
    - "Forbidden fonts list (Inter, Montserrat, Space Grotesk, Kode Mono) present in integra-brand.md"
    - "Forbidden colors list present in integra-brand.md"
    - "custom-brand.md references ui-ux-pro-max as the generator"
  artifacts:
    - path: "knowledge/design-systems/integra-brand.md"
      provides: "Official Integra brand design tokens"
      contains: "#FF6D49"
    - path: "knowledge/design-systems/custom-brand.md"
      provides: "Template for AI-generated custom design system"
      contains: "ui-ux-pro-max"
  key_links:
    - from: "knowledge/design-systems/integra-brand.md"
      to: "knowledge/design-system.md"
      via: "content derived from current design-system.md"
      pattern: "Coral|FF6D49"
    - from: "knowledge/design-systems/custom-brand.md"
      to: "skills/ui-ux-pro-max"
      via: "template filled by ui-ux-pro-max skill during build"
      pattern: "ui-ux-pro-max"
---

<objective>
Create the two-file branding architecture: knowledge/design-systems/integra-brand.md (official Integra brand from current design-system.md) and knowledge/design-systems/custom-brand.md (template for ui-ux-pro-max generated branding). Both files use identical section structure so downstream agents can consume either interchangeably.

Purpose: The branding toggle (WIZ-05) needs two design system files. Agents load only the relevant file based on .integra/config.json branding choice. This eliminates conditional logic in a single file (which Claude Code markdown doesn't support).
Output: Two new knowledge files in knowledge/design-systems/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@knowledge/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integra-brand.md from current design-system.md</name>
  <files>knowledge/design-systems/integra-brand.md</files>
  <read_first>
    - knowledge/design-system.md (source content -- copy and restructure into new location)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (Pattern 4: Two-File Branding Architecture, BRAND-01 through BRAND-06)
    - .planning/research/ARCHITECTURE.md (Section 5: Branding Toggle)
  </read_first>
  <action>
Create the directory `knowledge/design-systems/` and the file `knowledge/design-systems/integra-brand.md`.

The content is the current `knowledge/design-system.md` restructured with these specific section headings (which custom-brand.md will mirror):

```markdown
# Integra Official Design System

> Source: https://github.com/Integra-layer/integra-brand
> Live guide: https://integra-brand.vercel.app
> Drop-in kit: `kit/globals.css`, `kit/brand-tokens.css`, `kit/integra-logo.tsx`
> 
> This file is loaded when `.integra/config.json` has `"branding": "integra"`.
> For custom branding, see `knowledge/design-systems/custom-brand.md`.

***

## 1. Brand Identity

**Brand Name:** Integra
**Primary Color Name:** Coral
**Design Philosophy:** Dark-first, glassmorphic surfaces, purposeful motion, warm coral accents

***

## 2. Colors
```

Then copy ALL color tables from current design-system.md verbatim:
- Brand Colors table (Primary #FF6D49, Primary Dark #FC4E23, Primary Light #FF8A65, Gold #FFC17A, Pink #F34499, Pink Light #FFAFD6, Red #F71227, Teal #00A186)
- Status Colors table (Success #1FC16B, Warning #FA7319, Danger #FA3748, Info #335CFF)
- Neutral Scale table (White through Black #0A0A0F)
- Chart Colors list

Then add section:
```
### Forbidden Colors (NEVER use in Integra branding mode)

`#f97316`, `#ff6b6b`, `#22c55e`, `#eab308`, `#ef4444`, `#3b82f6`, `#6C5CE7`, `#A29BFE`

These are generic Tailwind/web defaults that conflict with the Integra brand.
```

```
## 3. Surfaces
```

Copy Dark Theme Surfaces and Light Theme Surfaces tables verbatim from current design-system.md.

Add:
```
### Forbidden Dark Backgrounds (NEVER use)

`#000000`, `#111111`, `#1a1a2e`, `#0d1117`, `#0A0A1A`, `#12122A`
```

```
## 4. Typography
```

Copy Fonts, Weights, and Scale tables verbatim from current design-system.md.

Add:
```
### Forbidden Fonts (NEVER substitute in Integra branding mode)

Inter, Montserrat, Space Grotesk, Kode Mono, Arial, Helvetica

These fonts are commonly used defaults. Integra uses Euclid Circular B exclusively for all UI text.
```

```
## 5. Gradients
```

Copy Gradients table and CSS example verbatim from current design-system.md.

```
## 6. Spacing and Borders
```

Copy Border Radius table, Shadows CSS, and Glassmorphism CSS verbatim from current design-system.md.

```
## 7. Animation
```

Copy Animation section verbatim from current design-system.md.

```
## 8. Logo
```

Copy Logo Files table, Rules, and React Component verbatim from current design-system.md.

```
## 9. Implementation
```

Copy Implementation section verbatim from current design-system.md.

```
## 10. Component Tokens (for shadcn/ui theming)

When using shadcn/ui components, apply these token mappings:
- `--primary`: #FF6D49 (Coral)
- `--primary-foreground`: #FFFFFF
- `--secondary`: #F34499 (Pink)
- `--accent`: #FFC17A (Gold)
- `--background`: #0A0A0F
- `--foreground`: #E6E6E6
- `--card`: rgba(23, 23, 28, 0.7)
- `--border`: rgba(255, 255, 255, 0.08)
- `--destructive`: #FA3748
- `--muted`: rgba(31, 31, 38, 0.4)
- `--muted-foreground`: #A3A3A3
- `--ring`: #FF6D49
- `--radius`: 12px
```

The section numbering (1-10) is critical -- custom-brand.md will use the same numbered sections.
  </action>
  <verify>
    <automated>test -f knowledge/design-systems/integra-brand.md && grep -c "#FF6D49" knowledge/design-systems/integra-brand.md | grep -q "[1-9]" && grep -c "Euclid Circular B" knowledge/design-systems/integra-brand.md | grep -q "[1-9]" && grep -c "Forbidden Fonts" knowledge/design-systems/integra-brand.md | grep -q "[1-9]" && grep -c "Forbidden Colors" knowledge/design-systems/integra-brand.md | grep -q "[1-9]" && grep -c "## 10\. Component Tokens" knowledge/design-systems/integra-brand.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at knowledge/design-systems/integra-brand.md
    - grep "#FF6D49" returns at least 1 match (Coral primary)
    - grep "Euclid Circular B" returns at least 1 match
    - grep "Forbidden Fonts" returns at least 1 match (BRAND-05)
    - grep "Inter, Montserrat, Space Grotesk, Kode Mono" returns at least 1 match (BRAND-05)
    - grep "Forbidden Colors" returns at least 1 match (BRAND-06)
    - grep "#6C5CE7" in Forbidden Colors section (old purple is forbidden)
    - Sections numbered 1-10 (grep "## 1\." through "## 10\." each return 1)
    - grep "Component Tokens" returns 1 match (section 10)
    - grep "branding.*integra" returns at least 1 match (config.json reference)
  </acceptance_criteria>
  <done>integra-brand.md exists with all content from current design-system.md restructured into 10 numbered sections, plus forbidden fonts/colors lists, plus component tokens for shadcn/ui theming. All Coral brand tokens present.</done>
</task>

<task type="auto">
  <name>Task 2: Create custom-brand.md template with matching section structure</name>
  <files>knowledge/design-systems/custom-brand.md</files>
  <read_first>
    - knowledge/design-systems/integra-brand.md (must mirror section headings exactly)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (Pattern 4: Two-File Branding Architecture, custom-brand.md template)
    - .planning/research/ARCHITECTURE.md (Section 5: custom-brand.md Content)
  </read_first>
  <action>
Create `knowledge/design-systems/custom-brand.md` with the exact same 10 section headings as integra-brand.md, but with placeholder content that the ui-ux-pro-max skill fills during the build phase.

```markdown
# Custom Design System

> Generated by the `ui-ux-pro-max` skill for [{project-name}]
> 
> This file is loaded when `.integra/config.json` has `"branding": "custom"`.
> For official Integra branding, see `knowledge/design-systems/integra-brand.md`.
>
> **How this file gets populated:** During the `/build` skill Phase 4 (UI Polish),
> if branding is "custom", the ui-ux-pro-max skill analyzes the project concept
> and generates a unique design system. It writes the results into this template.
> Until then, sections contain placeholder guidance.

***

## 1. Brand Identity

**Brand Name:** {project-name}
**Primary Color Name:** {to be generated by ui-ux-pro-max}
**Design Philosophy:** {to be generated -- should reflect the project's concept and target audience}

***

## 2. Colors

> ui-ux-pro-max generates a complete palette based on the project concept.
> Must include: primary, primary-dark, primary-light, accent-1, accent-2, accent-3.
> Must include: success, warning, danger, info status colors.
> Must include: neutral scale (at least 6 shades from light to dark).

| Name | Hex | CSS Variable | Usage |
|------|-----|-------------|-------|
| Primary | {generated} | `--brand` | {generated} |
| Primary Dark | {generated} | `--brand-dark` | Hover and active states |
| Primary Light | {generated} | `--brand-light` | Subtle accents |
| Accent 1 | {generated} | `--brand-accent-1` | {generated} |
| Accent 2 | {generated} | `--brand-accent-2` | {generated} |
| Accent 3 | {generated} | `--brand-accent-3` | {generated} |

### Status Colors

| Name | Hex | Usage |
|------|-----|-------|
| Success | {generated or default #1FC16B} | Positive states |
| Warning | {generated or default #FA7319} | Caution states |
| Danger | {generated or default #FA3748} | Error states |
| Info | {generated or default #335CFF} | Informational |

### Neutral Scale

| Name | Hex |
|------|-----|
| {generated -- at least 6 shades from lightest to darkest} |

***

## 3. Surfaces

> Dark theme is the default. Light theme is optional.
> ui-ux-pro-max generates surface colors that complement the brand palette.

### Dark Theme Surfaces

| Layer | Value | Usage |
|-------|-------|-------|
| Background | {generated} | Page background |
| Card/Surface | {generated} | Cards, panels |
| Elevated | {generated} | Elevated surfaces |
| Border | {generated} | Dividers, outlines |
| Text | {generated} | Primary text |
| Text Muted | {generated} | Secondary text |

### Light Theme Surfaces (optional)

| Layer | Value | Usage |
|-------|-------|-------|
| Background | {generated} | Page background |
| Card/Surface | {generated} | Cards, panels |
| Text | {generated} | Primary text |

***

## 4. Typography

> ui-ux-pro-max selects fonts that match the project personality.
> Custom branding is NOT restricted to Euclid Circular B -- any font is allowed.
> However, the chosen font must be a web font available via Google Fonts or similar CDN.

### Fonts

| Type | Stack |
|------|-------|
| Sans (Primary) | {generated -- e.g., "Plus Jakarta Sans", system-ui, sans-serif} |
| Monospace | {generated or default "Geist Mono", ui-monospace, monospace} |

### Weights

{generated -- typically 400 Regular, 500 Medium, 600 Semibold, 700 Bold}

### Scale

| Name | Size | Usage |
|------|------|-------|
| {generated -- follow standard type scale pattern} |

***

## 5. Gradients

> ui-ux-pro-max generates 2-4 gradients from the brand palette.

| Name | CSS | Usage |
|------|-----|-------|
| Brand | {generated from primary colors} | Hero, CTAs |
| Button | {generated} | Primary buttons |
| Text | {generated} | Gradient text headings |

***

## 6. Spacing and Borders

### Border Radius

| sm | md | lg (default) | xl | 2xl | full |
|----|----|----|----|----|------|
| {generated or default: 6px, 8px, 12px, 16px, 24px, 9999px} |

### Shadows

{generated -- 3-4 shadow levels from subtle to pronounced}

### Glassmorphism (if applicable)

{generated -- backdrop-filter and semi-transparent background}

***

## 7. Animation

> Keep animations purposeful and subtle. No flashy effects.

- Hover: {generated or default translateY(-2px), 150ms ease}
- Transitions: {generated or default 150-200ms}
- {additional motion patterns if appropriate for the project}

***

## 8. Logo

> The project's logo is generated or provided by the user.
> ui-ux-pro-max may suggest logo concepts but does not generate image files.

{placeholder -- user provides or describes desired logo}

***

## 9. Implementation

> After ui-ux-pro-max fills this template, the executor agent converts these tokens into:
> 1. `src/app/globals.css` with CSS custom properties
> 2. `tailwind.config.ts` theme extension
> 3. shadcn/ui theme overrides

***

## 10. Component Tokens (for shadcn/ui theming)

> These map to shadcn/ui's CSS variable system.
> ui-ux-pro-max populates from the palette in Section 2.

- `--primary`: {from Section 2 Primary}
- `--primary-foreground`: {contrast color for primary}
- `--secondary`: {from Section 2 Accent 1}
- `--accent`: {from Section 2 Accent 2}
- `--background`: {from Section 3 Background}
- `--foreground`: {from Section 3 Text}
- `--card`: {from Section 3 Card/Surface}
- `--border`: {from Section 3 Border}
- `--destructive`: {from Section 2 Danger}
- `--muted`: {from Section 3 Muted or secondary surface}
- `--muted-foreground`: {from Section 3 Text Muted}
- `--ring`: {from Section 2 Primary}
- `--radius`: {from Section 6 lg default}
```

This template is NOT deleted or overwritten globally. Each generated project gets a COPY of this template in its own `knowledge/design-systems/custom-brand.md` or the ui-ux-pro-max skill writes directly into the project's design system file.
  </action>
  <verify>
    <automated>test -f knowledge/design-systems/custom-brand.md && grep -c "ui-ux-pro-max" knowledge/design-systems/custom-brand.md | grep -q "[1-9]" && grep -c "## 1\. Brand Identity" knowledge/design-systems/custom-brand.md | grep -q "1" && grep -c "## 10\. Component Tokens" knowledge/design-systems/custom-brand.md | grep -q "1" && grep -c "branding.*custom" knowledge/design-systems/custom-brand.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at knowledge/design-systems/custom-brand.md
    - grep "ui-ux-pro-max" returns at least 2 matches (referenced as generator)
    - Sections numbered 1-10, matching integra-brand.md headings exactly
    - grep "## 1. Brand Identity" returns 1 match
    - grep "## 2. Colors" returns 1 match
    - grep "## 3. Surfaces" returns 1 match
    - grep "## 4. Typography" returns 1 match
    - grep "## 5. Gradients" returns 1 match
    - grep "## 6. Spacing" returns 1 match
    - grep "## 7. Animation" returns 1 match
    - grep "## 8. Logo" returns 1 match
    - grep "## 9. Implementation" returns 1 match
    - grep "## 10. Component Tokens" returns 1 match
    - grep "branding.*custom" returns at least 1 match (config.json reference)
    - Custom branding does NOT have forbidden fonts list (any font allowed per BRAND-02)
  </acceptance_criteria>
  <done>custom-brand.md template exists with 10 numbered sections matching integra-brand.md structure. All sections have placeholder guidance for ui-ux-pro-max to fill. References ui-ux-pro-max as generator. No font restrictions (custom branding can use any font). Same output format as integra-brand.md (BRAND-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Knowledge files are static markdown consumed by agents. No user input processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | knowledge/design-systems/*.md | accept | Knowledge files are checked into git and only modified by skills during project generation. No runtime user input reaches these files. |
</threat_model>

<verification>
- `test -f knowledge/design-systems/integra-brand.md` passes
- `test -f knowledge/design-systems/custom-brand.md` passes
- Both files have sections numbered 1-10 with matching headings
- integra-brand.md contains #FF6D49, Euclid Circular B, forbidden fonts, forbidden colors
- custom-brand.md references ui-ux-pro-max, has placeholder content, allows any font
- Section heading comparison: `diff <(grep "^## " knowledge/design-systems/integra-brand.md) <(grep "^## " knowledge/design-systems/custom-brand.md)` shows identical headings
</verification>

<success_criteria>
- integra-brand.md has complete Integra design system with all tokens from design-system.md
- custom-brand.md is a fillable template with matching structure
- Both produce same output format (BRAND-03): numbered sections, CSS variables, Tailwind config
- Forbidden fonts enforced in integra-brand.md (BRAND-05)
- Forbidden colors enforced in integra-brand.md (BRAND-06)
- custom-brand.md has no font/color restrictions (BRAND-02 -- custom = AI-generated unique design)
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ux-branding-toggle/02-03-SUMMARY.md`
</output>
