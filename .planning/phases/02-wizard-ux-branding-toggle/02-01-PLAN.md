---
phase: 02-wizard-ux-branding-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/start/SKILL.md
  - agents/discovery.md
autonomous: true
requirements: [WIZ-04, WIZ-05, WIZ-06, WIZ-07, WIZ-08]
must_haves:
  truths:
    - "Start wizard asks open-ended questions for person-first discovery (Q1-Q4)"
    - "Start wizard asks selectable network question with 3 options after discovery"
    - "Start wizard asks selectable branding question with 2 options after network"
    - "Start wizard asks selectable UI generation question with 3 options after branding"
    - "Discovery agent has ZERO AskUserQuestion calls and receives pre-collected answers"
    - "Discovery agent produces a Discovery Brief with network, branding, and uiGeneration fields"
  artifacts:
    - path: "skills/start/SKILL.md"
      provides: "Rewritten wizard with hybrid open-ended + selectable flow"
      contains: "PHASE 1.5: TECHNICAL CONFIGURATION"
    - path: "agents/discovery.md"
      provides: "Non-interactive context analyzer"
      contains: "Context analyzer"
  key_links:
    - from: "skills/start/SKILL.md"
      to: "agents/discovery.md"
      via: "passes collected answers as input to subagent"
      pattern: "discovery agent.*receives|pass.*answers"
---

<objective>
Rewrite the start wizard to add network selector, branding toggle, and UI generation method as selectable questions after the open-ended person-first discovery phase. Convert the discovery agent from an interactive questionnaire to a non-interactive context analyzer that receives pre-collected answers and produces a Discovery Brief.

Purpose: The start wizard owns ALL AskUserQuestion calls (subagents cannot call AskUserQuestion -- GitHub #29547). Technical configuration must be selectable with numbered options while person-first discovery stays open-ended.
Output: Rewritten skills/start/SKILL.md and agents/discovery.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@skills/start/SKILL.md
@agents/discovery.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite start wizard with hybrid flow and 3 new selectable questions</name>
  <files>skills/start/SKILL.md</files>
  <read_first>
    - skills/start/SKILL.md (current wizard -- preserve Phase 1 Q1-Q4 open-ended flow exactly)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (Pattern 2: Hybrid Discovery Flow, Code Examples section)
    - .planning/research/ARCHITECTURE.md (Section 4: Selectable-Only Wizard, Section 5: Branding Toggle)
    - knowledge/design-system.md (verify brand name references for branding question text)
  </read_first>
  <action>
Rewrite skills/start/SKILL.md with this structure. Preserve ALL existing content in Phase 1 (Q1-Q5) and Phase 2-3 exactly as they are now. The changes are INSERTIONS, not replacements of the person-first flow.

**Keep unchanged:** The frontmatter, CRITICAL RULES section, PHASE 1 (Q1 through Q5), ANTI-PATTERNS section. These stay word-for-word identical.

**Insert new section "PHASE 1.5: TECHNICAL CONFIGURATION" between Phase 1 (Q5) and Phase 2 (SYNTHESIS).** This section contains exactly 3 selectable questions:

**Q6 -- Network Selection:**
```
Use AskUserQuestion:

"Which network are you building for?

1. Mainnet (production-ready, Chain ID 26217, real IRL tokens)
2. Testnet (development, Chain ID 26218, free IRL from faucet) (recommended)
3. Both (separate configs for each)"
```

**Q7 -- Branding Toggle:**
```
Use AskUserQuestion:

"Design approach for your dApp?

1. Integra official branding (Coral palette, Euclid Circular B font, dark theme)
2. Custom design (AI generates a unique look based on your concept)"
```

**Q8 -- UI Generation Method:**
```
Use AskUserQuestion:

"How should we build the UI?

1. Generate with Stitch AI (Google's design tool creates screens, we refine)
2. Build manually (component-by-component with shadcn/ui)
3. Decide later (skip for now, choose during build phase)"
```

**Also insert Q5b -- Scope (split from current Q5).** Current Q5 combines scope + name in one question. Split it:
- Q5 stays as name question (keep the current text but remove scope part)
- Q5b becomes a selectable scope question BEFORE Q6:
```
Use AskUserQuestion:

"How ambitious should we go?

1. Focused prototype (1-2 days, core concept only)
2. Solid MVP (3-5 days, usable product)
3. Full vision (1-2 weeks, complete feature set)"
```

**Modify PHASE 2: SYNTHESIS** -- Update the Discovery Brief call to pass ALL collected answers (Q1-Q8) to the discovery agent as a structured input. Add this instruction after the "Once confirmed" line in Step 2:

"Pass the following to the discovery agent as structured input (the agent is non-interactive -- it produces a Discovery Brief without asking questions):
- background: Q1 answer
- painPoint: Q2 answer
- spark: Q3 answer
- idea: Q4 answer (if asked)
- name: Q5 answer
- scope: Q5b answer (prototype/mvp/full)
- network: Q6 answer (mainnet/testnet/both)
- branding: Q7 answer (integra/custom)
- uiGeneration: Q8 answer (stitch/manual/deferred)"

**Modify PHASE 3: PROJECT SETUP Step 2 -- Scaffold** -- Add `.integra/config.json` to the list of files created during scaffold:
```
- `.integra/config.json` -- project configuration from wizard choices:
  {
    "network": "testnet",
    "branding": "integra",
    "uiGeneration": "manual",
    "scope": "mvp",
    "name": "My dApp"
  }
```

**Remove the existing NETWORK SELECTION section** (lines 97-104 in current file) since it is now part of Phase 1.5 Q6.

**Update the Location question (Phase 3 Step 1)** to be selectable:
```
Use AskUserQuestion:

"Where should I create the project?

1. Default location (~/projects/{name-kebab-case})
2. Let me specify a custom path"
```
  </action>
  <verify>
    <automated>grep -c "PHASE 1.5: TECHNICAL CONFIGURATION" skills/start/SKILL.md | grep -q "1" && grep -c "Which network are you building for" skills/start/SKILL.md | grep -q "1" && grep -c "Design approach for your dApp" skills/start/SKILL.md | grep -q "1" && grep -c "How should we build the UI" skills/start/SKILL.md | grep -q "1" && grep -c "How ambitious should we go" skills/start/SKILL.md | grep -q "1" && grep -c "integra/config.json" skills/start/SKILL.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "PHASE 1.5: TECHNICAL CONFIGURATION" skills/start/SKILL.md returns 1 match
    - grep "Which network are you building for" skills/start/SKILL.md returns 1 match
    - grep "Design approach for your dApp" skills/start/SKILL.md returns 1 match
    - grep "How should we build the UI" skills/start/SKILL.md returns 1 match
    - grep "How ambitious should we go" skills/start/SKILL.md returns 1 match
    - grep "integra/config.json" skills/start/SKILL.md returns at least 1 match
    - grep "PHASE 1: UNDERSTAND THE PERSON" skills/start/SKILL.md returns 1 match (preserved)
    - grep "Tell me a bit about yourself" skills/start/SKILL.md returns 1 match (Q1 preserved)
    - The old standalone "NETWORK SELECTION" section (with "Build for mainnet or testnet?") is removed
    - grep -c "Build for mainnet or testnet" skills/start/SKILL.md returns 0
  </acceptance_criteria>
  <done>Start wizard has Phase 1 (open-ended Q1-Q5), Phase 1.5 (selectable Q5b, Q6, Q7, Q8), Phase 2 (synthesis with structured discovery agent input), Phase 3 (scaffold with .integra/config.json). Old network selection section removed.</done>
</task>

<task type="auto">
  <name>Task 2: Convert discovery agent to non-interactive context analyzer</name>
  <files>agents/discovery.md</files>
  <read_first>
    - agents/discovery.md (current interactive questionnaire -- understand output format)
    - .planning/phases/02-wizard-ux-branding-toggle/02-RESEARCH.md (Pattern 3: Non-Interactive Discovery Agent, Discovery Brief output format)
    - .planning/research/ARCHITECTURE.md (Section 4: AskUserQuestion constraints)
  </read_first>
  <action>
Rewrite agents/discovery.md completely. The new version:

**Frontmatter:**
```yaml
- - -
name: discovery
description: Context analyzer -- produces Discovery Brief from wizard-collected answers
model: sonnet
tools: [Read, Grep, Glob]
- - -
```

**Header:**
```
# Discovery Agent

You are the Discovery agent for Integra Developer Studio. You are a NON-INTERACTIVE context analyzer. You receive pre-collected answers from the /start wizard and produce a structured Discovery Brief that downstream agents (architect, contract-designer, frontend-designer, integration-planner) consume.

**CRITICAL: You MUST NOT call AskUserQuestion.** You run as a subagent -- AskUserQuestion calls from subagents fail silently (GitHub #29547). All user interaction happens in the /start wizard before you are invoked.
```

**Input Format section:**
```
## Input

You receive structured input from the /start wizard with these fields:

- **background**: Who the user is (from Q1 -- open-ended)
- **painPoint**: What frustrates them (from Q2 -- open-ended)
- **spark**: Connection to Integra capability (from Q3 -- open-ended)
- **idea**: Crystallized idea (from Q3-Q4 -- open-ended)
- **name**: dApp name (from Q5)
- **scope**: Prototype | MVP | Full (from Q5b -- selectable)
- **network**: mainnet | testnet | both (from Q6 -- selectable)
- **branding**: integra | custom (from Q7 -- selectable)
- **uiGeneration**: stitch | manual | deferred (from Q8 -- selectable)
```

**Analysis Rules section:**
```
## Analysis Rules

1. **Infer category** from the idea and background. Map to: DeFi, NFT/Digital Assets, DAO/Governance, Identity/Reputation, Social/Content, Gaming/GameFi, Real World Assets, Infrastructure/Tooling, AI Agents.

2. **Infer target users** from the background and pain point. Be specific: "retail investors who want fractional property ownership" not "general public."

3. **Auto-select Integra features** based on the idea:
   - Asset Passport: if the idea involves tokenizing real-world assets
   - Wrapper/IRWA: if assets need to be tradeable as ERC-20/721/3643 tokens
   - GOB: if the idea involves trading, marketplace, or order book
   - AgentAuth: if AI agents interact with the dApp
   - XP System: ALWAYS include (every dApp benefits from engagement tracking)
   - Subdomain: ALWAYS include (every dApp gets a .integralayer.com subdomain)

4. **Infer complexity** from scope:
   - Prototype -> Simple (1-2 contracts, basic UI)
   - MVP -> Medium (3-5 contracts, full UI)
   - Full -> Complex (6+ contracts, advanced UI)

5. **Read knowledge files** for context:
   - `knowledge/networks/mainnet.md` or `knowledge/networks/testnet.md` based on network choice
   - `knowledge/design-systems/integra-brand.md` if branding is "integra"
   - `knowledge/integra-ecosystem.md` for product references
```

**Output Format section:**
```
## Output Format

Produce a Discovery Brief in this exact format:

## Discovery Brief

**Generated from wizard answers -- no user interaction in this agent**

- **Category**: {inferred from idea + background}
- **Idea**: {one-sentence description synthesized from spark + idea}
- **Target Users**: {inferred from background + pain point -- be specific}
- **Background Context**: {key insight from the person's background that makes this idea unique}
- **Integra Features**: {comma-separated list of auto-selected features with brief WHY for each}
- **Complexity**: {Simple | Medium | Complex -- inferred from scope}
- **Scope**: {Prototype | MVP | Full -- from wizard input}
- **dApp Name**: {from wizard input}
- **Network**: {mainnet | testnet | both -- from wizard input}
- **Branding**: {integra | custom -- from wizard input}
- **UI Generation**: {stitch | manual | deferred -- from wizard input}
- **Notes**: {any inferences, recommendations, or flags for downstream agents}

This brief is consumed by the architect, contract-designer, frontend-designer, and integration-planner agents. Every field must be filled.
```

**Remove ALL of the following from the current file:**
- The "Questionnaire Flow" section (8 interactive questions)
- The "Rules" section about skipping questions
- The "Personality" section (replace with analysis-focused personality)
- The "Integra Ecosystem Knowledge" section (agent reads knowledge files instead)
- Any reference to AskUserQuestion
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" agents/discovery.md | grep -q "^0$" && grep -c "Context analyzer" agents/discovery.md | grep -q "1" && grep -c "Discovery Brief" agents/discovery.md | grep -q "[1-9]" && grep -c "MUST NOT call AskUserQuestion" agents/discovery.md | grep -q "1" && grep -c "network.*mainnet.*testnet" agents/discovery.md | grep -q "[1-9]" && grep -c "branding.*integra.*custom" agents/discovery.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "AskUserQuestion" agents/discovery.md returns exactly 0
    - grep "Context analyzer" agents/discovery.md returns at least 1 match
    - grep "MUST NOT call AskUserQuestion" agents/discovery.md returns 1 match
    - grep "Discovery Brief" agents/discovery.md returns at least 1 match
    - Output format includes network, branding, uiGeneration fields
    - No "Questionnaire Flow" section remains
    - Frontmatter description says "Context analyzer"
  </acceptance_criteria>
  <done>Discovery agent is a non-interactive context analyzer with zero AskUserQuestion calls. Receives structured input from start wizard. Produces Discovery Brief with all new fields (network, branding, uiGeneration, scope).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> wizard | Free-text answers from open-ended questions enter the system |
| Wizard -> discovery agent | Structured answers passed to subagent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | skills/start/SKILL.md | accept | Wizard collects text input via AskUserQuestion; downstream agents treat all input as untrusted context, not executable commands. Low risk -- markdown skill files, not code. |
| T-02-02 | Information Disclosure | .integra/config.json | accept | Config contains only enum values (network, branding, scope) and a project name. No secrets. Written to user's local project directory. |
</threat_model>

<verification>
- `grep -c "PHASE 1.5: TECHNICAL CONFIGURATION" skills/start/SKILL.md` returns 1
- `grep -c "AskUserQuestion" agents/discovery.md` returns 0
- `grep -c "Which network are you building for" skills/start/SKILL.md` returns 1
- `grep -c "Design approach for your dApp" skills/start/SKILL.md` returns 1
- `grep -c "How should we build the UI" skills/start/SKILL.md` returns 1
- `grep -c "Tell me a bit about yourself" skills/start/SKILL.md` returns 1 (Q1 preserved)
- `grep -c "integra/config.json" skills/start/SKILL.md` returns at least 1
- `grep -c "Build for mainnet or testnet" skills/start/SKILL.md` returns 0 (old section removed)
</verification>

<success_criteria>
- Start wizard has hybrid flow: open-ended Phase 1, selectable Phase 1.5, synthesis Phase 2
- Three new selectable questions: network (3 options), branding (2 options), UI method (3 options)
- Scope split from Q5 into its own selectable question Q5b
- Discovery agent is fully non-interactive with zero AskUserQuestion calls
- Discovery Brief output includes network, branding, uiGeneration fields
- .integra/config.json included in project scaffold
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ux-branding-toggle/02-01-SUMMARY.md`
</output>
