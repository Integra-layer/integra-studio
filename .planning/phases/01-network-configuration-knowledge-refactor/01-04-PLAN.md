---
phase: 01-network-configuration-knowledge-refactor
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - agents/architect.md
  - agents/deployer.md
  - agents/executor.md
  - agents/frontend-designer.md
  - agents/integration-planner.md
  - agents/reviewer.md
  - skills/start/SKILL.md
  - skills/deploy/SKILL.md
  - skills/review/SKILL.md
  - skills/explore/SKILL.md
autonomous: true
requirements:
  - NET-02
  - NET-14
  - CONFIG-08
  - CONFIG-09

must_haves:
  truths:
    - "All 6 agents with network refs (architect, deployer, executor, frontend-designer, integration-planner, reviewer) reference knowledge/networks/ instead of inline config"
    - "No agent or skill contains testnet-rpc.integralayer.com"
    - "No agent or skill contains #6C5CE7 (old purple) or Inter font"
    - "Deploy skill supports --network mainnet|testnet target selection"
    - "Start skill chain ID reference updated to mention both networks"
  artifacts:
    - path: "agents/architect.md"
      provides: "Agent with knowledge/networks/ pointer"
      contains: "knowledge/networks/"
    - path: "agents/deployer.md"
      provides: "Agent with dual-network deployment support"
      contains: "knowledge/networks/"
    - path: "agents/executor.md"
      provides: "Agent with knowledge/networks/ pointer and correct design refs"
      contains: "knowledge/networks/"
    - path: "agents/frontend-designer.md"
      provides: "Agent with correct Integra design system colors and font"
      contains: "#FF6D49"
    - path: "agents/integration-planner.md"
      provides: "Agent with knowledge pointer and correct block time"
      contains: "~5"
    - path: "agents/reviewer.md"
      provides: "Agent with correct design checklist and chain ID refs"
      contains: "knowledge/networks/"
    - path: "skills/deploy/SKILL.md"
      provides: "Deploy skill with mainnet/testnet target"
      contains: "mainnet"
  key_links:
    - from: "agents/*.md"
      to: "knowledge/networks/"
      via: "knowledge pointer directive"
      pattern: "knowledge/networks/"
    - from: "skills/deploy/SKILL.md"
      to: "knowledge/networks/"
      via: "network target selection"
      pattern: "mainnet.*testnet"
---

<objective>
Update all 6 agents and 4 skills to reference knowledge/networks/ instead of inline network config. Fix design system references in agents. Add dual-network deployment support to deploy skill.

Purpose: Agents and skills currently hardcode network data (wrong chain IDs, deprecated RPC URLs, old design system). After this plan, they all read from the single source of truth in knowledge/networks/ and use the correct Integra brand design system.

Output: 6 updated agents, 4 updated skills -- all pointing to knowledge/networks/, all free of deprecated values
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md
@knowledge/networks/mainnet.md
@knowledge/networks/testnet.md
@knowledge/networks/shared.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 6 agents to reference knowledge/networks/ and fix design system</name>
  <files>agents/architect.md, agents/deployer.md, agents/executor.md, agents/frontend-designer.md, agents/integration-planner.md, agents/reviewer.md</files>
  <read_first>
    - agents/architect.md (entire file)
    - agents/deployer.md (entire file)
    - agents/executor.md (entire file)
    - agents/frontend-designer.md (entire file)
    - agents/integration-planner.md (entire file)
    - agents/reviewer.md (entire file)
    - .planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md (lines 50-113 for per-agent contradictions)
  </read_first>
  <action>
For EACH of the 6 agents, apply these changes. Read each file fully before modifying.

**Pattern: Replace inline network config with knowledge pointer**

In each agent that has inline chain IDs, RPC URLs, or explorer URLs, replace those sections with this standard block:

```markdown
## Network Configuration

Read `knowledge/networks/mainnet.md` or `knowledge/networks/testnet.md` based on the project's target network. Read `knowledge/networks/shared.md` for token info (IRL/airl), WIRL address, precompile addresses, and decimal conversion warnings. Never hardcode chain IDs, RPC URLs, or explorer URLs.
```

**Per-agent specifics:**

1. **agents/architect.md:**
   - Remove inline RPC/explorer/chainID (research says lines 42-47)
   - Add knowledge/networks/ pointer block above
   - If it references "Inter" font, replace with "Euclid Circular B"
   - If it references chain ID "26217" as testnet, remove it (26217 is mainnet)

2. **agents/deployer.md:**
   - Remove inline network config block (research says lines 36-61)
   - Add knowledge/networks/ pointer block
   - Replace any `testnet-rpc.integralayer.com` with instruction to read from knowledge/networks/
   - Replace any `testnet-explorer.integralayer.com` or `explorer.integralayer.com` with `blockscout.integralayer.com` reference via knowledge pointer
   - Add dual-network awareness: "Check project's target network to determine deployment target"

3. **agents/executor.md:**
   - Remove inline Web3Auth config section (research says lines 166-186)
   - Remove inline chain config section (research says lines 188-209)
   - Add knowledge/networks/ pointer block
   - Fix any `#6C5CE7` to `#FF6D49`
   - Fix any "Inter" font to "Euclid Circular B"
   - Fix any `testnet-rpc.integralayer.com` references
   - Fix any `explorer.integralayer.com` to pointer

4. **agents/frontend-designer.md:**
   - Replace entire design system section (research says lines 23-50) with correct Integra brand:
     - Primary: Coral `#FF6D49`, not indigo-500 or `#6366f1`
     - Background: `#0A0A0F`, not `#0a0a0f` (case match) or `#0a0a1a`
     - Card: `rgba(23, 23, 28, 0.7)`, not `#12121a`
     - Font: "Euclid Circular B", not "Inter"
   - Add knowledge/networks/ pointer if any network data exists

5. **agents/integration-planner.md:**
   - Remove inline network config (research says lines 24-31)
   - Fix block time from "~2 seconds" to "~5 seconds" (per CONFIG-10)
   - Add knowledge/networks/ pointer block

6. **agents/reviewer.md:**
   - Fix design system checklist (research says lines 72-77):
     - Colors should reference `#FF6D49` (Coral), not `indigo-500` or `#6366f1`
     - Background should be `#0A0A0F`, not `#0a0a0f` or `#0a0a1a`
     - Card should be `rgba(23, 23, 28, 0.7)`, not `#12121a`
     - Font should be "Euclid Circular B", not "Inter"
   - Fix chain ID reference (research says line 86): ensure it says to verify against knowledge/networks/ not a hardcoded value
   - Add knowledge/networks/ pointer if not present

**CRITICAL for all agents:**
- Do NOT remove non-network content. Each agent has domain-specific instructions (architecture patterns, deployment steps, code quality rules, etc.) that must remain untouched.
- Only modify the sections that contain network config or design system data.
- After changes, every agent should contain the string "knowledge/networks/" at least once.
  </action>
  <verify>
    <automated>AGENT_COUNT=$(grep -rl "knowledge/networks/" agents/*.md 2>/dev/null | wc -l | tr -d ' ') && test "$AGENT_COUNT" -ge 6 && ! grep -rl "testnet-rpc.integralayer.com" agents/*.md 2>/dev/null && ! grep -rl "#6C5CE7" agents/*.md 2>/dev/null && ! grep -l "~2 seconds" agents/integration-planner.md 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - At least 6 agent files contain "knowledge/networks/"
    - Zero agent files contain "testnet-rpc.integralayer.com"
    - Zero agent files contain "#6C5CE7" (old purple)
    - agents/integration-planner.md does NOT contain "~2 seconds" (corrected to ~5 seconds)
    - agents/frontend-designer.md contains "#FF6D49" or references Coral
    - agents/frontend-designer.md contains "Euclid Circular B" (not "Inter" as heading/body font)
    - agents/reviewer.md design checklist references correct brand colors
    - agents/executor.md does NOT contain inline Web3Auth chain config block
    - agents/deployer.md mentions both mainnet and testnet deployment
  </acceptance_criteria>
  <done>All 6 agents point to knowledge/networks/ for network data, have correct design system references, and contain zero deprecated values</done>
</task>

<task type="auto">
  <name>Task 2: Update skills (start, deploy, review, explore) for dual-network</name>
  <files>skills/start/SKILL.md, skills/deploy/SKILL.md, skills/review/SKILL.md, skills/explore/SKILL.md</files>
  <read_first>
    - skills/start/SKILL.md (entire file)
    - skills/deploy/SKILL.md (entire file)
    - skills/review/SKILL.md (entire file)
    - skills/explore/SKILL.md (entire file)
    - .planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md (lines 165-169 for per-skill changes)
  </read_first>
  <action>
Read each skill file fully before modifying.

**skills/start/SKILL.md:**
- Find the chain ID reference at line 158 (research says it hardcodes 26218 for testnet)
- Replace with a reference that mentions both networks: "Chain ID 26217 (mainnet) or 26218 (testnet) -- see knowledge/networks/"
- Add a note that the wizard should ask the user to select mainnet or testnet (per NET-02). Add this near the top of the wizard flow or near the chain reference:
  ```
  Early in the wizard, ask: "Build for mainnet or testnet?" (default: mainnet)
  Use the selection to determine which knowledge/networks/ file to reference throughout.
  ```

**skills/deploy/SKILL.md:**
- This skill currently hardcodes testnet deployment throughout
- Add dual-network support: the skill should check the project's target network and deploy accordingly
- Replace any `testnet-rpc.integralayer.com` with `knowledge/networks/` reference
- Replace any hardcoded chain ID (26217 used wrongly as testnet) with reference to knowledge/networks/
- Add support for `--network mainnet|testnet` target selection (per CONFIG-09):
  ```
  ## Network Target
  
  Read the project's `.integra/config.json` for the target network, or accept a `--network` flag.
  Default: testnet. For mainnet deployment, the skill should:
  - Read knowledge/networks/mainnet.md for chain ID, RPC URL, gas price
  - Warn about mainnet gas costs (min 5 Twei)
  - Use sapphire_mainnet for Web3Auth verification
  ```
- Replace any `explorer.integralayer.com` or `testnet-explorer.integralayer.com` with `blockscout.integralayer.com` via knowledge pointer

**skills/review/SKILL.md:**
- Fix chain ID reference at line 91 (research says it uses 26217 in a testnet context)
- Replace with: "Verify chain ID against knowledge/networks/mainnet.md (26217) or knowledge/networks/testnet.md (26218) based on project target"
- Add knowledge/networks/ pointer

**skills/explore/SKILL.md:**
- Verify faucet info. If it mentions "1000 IRL", fix to "10 IRL + 1,000 tUSDI per request"
- Add knowledge/networks/ pointer if any network data is inline

**CRITICAL for all skills:**
- Do NOT remove non-network content (wizard flow logic, deployment steps, review criteria, explore navigation)
- Only modify sections with hardcoded network data or incorrect values
  </action>
  <verify>
    <automated>grep -q "knowledge/networks/" skills/start/SKILL.md && grep -q "knowledge/networks/" skills/deploy/SKILL.md && grep -q "knowledge/networks/" skills/review/SKILL.md && ! grep -rl "testnet-rpc.integralayer.com" skills/*/SKILL.md 2>/dev/null && grep -qi "mainnet" skills/deploy/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/start/SKILL.md contains "knowledge/networks/" pointer
    - skills/start/SKILL.md mentions both mainnet and testnet as wizard options (NET-02)
    - skills/deploy/SKILL.md contains "knowledge/networks/" pointer
    - skills/deploy/SKILL.md mentions both "mainnet" and "testnet" deployment targets (CONFIG-09)
    - skills/deploy/SKILL.md mentions gas cost warning for mainnet
    - skills/review/SKILL.md contains "knowledge/networks/" pointer
    - skills/review/SKILL.md does NOT hardcode a single chain ID as the only valid one
    - Zero skill files contain "testnet-rpc.integralayer.com"
    - skills/explore/SKILL.md faucet info is "10 IRL" not "1000 IRL" (if faucet was mentioned)
  </acceptance_criteria>
  <done>All 4 skills reference knowledge/networks/, deploy supports dual-network target, start mentions network selection, review validates against knowledge, no deprecated URLs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent prompts to generated code | Agents use their instructions to generate project code; wrong network config in agent prompts means wrong config in every generated dApp |
| Deploy skill to chain | Deploy skill determines which chain receives the contract deployment; wrong target = wrong chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Deploy skill mainnet deployment | mitigate | Deploy skill must warn about mainnet gas costs and confirm network target before deployment. Acceptance criteria require gas cost warning. |
| T-01-09 | Tampering | Agent network references | mitigate | All 6 agents reference knowledge/networks/ instead of inline values. If knowledge files are correct (Plan 01), agents produce correct output. Grep verification ensures no stale inline values remain. |
</threat_model>

<verification>
```bash
# All agents reference knowledge/networks/
AGENT_HITS=$(grep -rl "knowledge/networks/" agents/*.md | wc -l | tr -d ' ')
test "$AGENT_HITS" -ge 6

# All modified skills reference knowledge/networks/
grep -q "knowledge/networks/" skills/start/SKILL.md
grep -q "knowledge/networks/" skills/deploy/SKILL.md
grep -q "knowledge/networks/" skills/review/SKILL.md

# Zero deprecated URLs across agents and skills
! grep -r "testnet-rpc.integralayer.com" agents/ skills/

# Zero old purple across agents
! grep -r "#6C5CE7" agents/

# Deploy skill has dual-network
grep -qi "mainnet" skills/deploy/SKILL.md
```
</verification>

<success_criteria>
- All 6 network-referencing agents contain "knowledge/networks/" and zero deprecated values
- frontend-designer and reviewer agents have correct Integra design system (Coral, Euclid)
- integration-planner has correct block time (~5s not ~2s)
- Deploy skill supports mainnet and testnet targets with appropriate warnings
- Start skill mentions network selection for the wizard
- Review skill validates against knowledge/networks/ not hardcoded chain IDs
- Zero occurrences of testnet-rpc.integralayer.com in any agent or skill
</success_criteria>

<output>
After completion, create `.planning/phases/01-network-configuration-knowledge-refactor/01-04-SUMMARY.md`
</output>
