---
phase: 01-network-configuration-knowledge-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/CLAUDE.md
  - knowledge/integra-ecosystem.md
  - knowledge/product-ideas.md
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - NET-14

must_haves:
  truths:
    - "CLAUDE.md has no inline chain IDs, RPC URLs, or explorer URLs -- only a pointer to knowledge/networks/"
    - ".claude/CLAUDE.md design system matches root CLAUDE.md (Coral #FF6D49, Euclid Circular B, no purple, no Inter)"
    - "integra-ecosystem.md has no network config section -- only products, APIs, contracts"
    - "Faucet amount corrected to 10 IRL + 1,000 tUSDI everywhere"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Refactored root config with knowledge pointers"
      contains: "knowledge/networks/"
    - path: ".claude/CLAUDE.md"
      provides: "Synced config matching root CLAUDE.md design system"
      contains: "#FF6D49"
    - path: "knowledge/integra-ecosystem.md"
      provides: "Products and APIs only, no network config"
    - path: "knowledge/product-ideas.md"
      provides: "Corrected faucet amount"
  key_links:
    - from: "CLAUDE.md"
      to: "knowledge/networks/"
      via: "pointer directive"
      pattern: "knowledge/networks/"
    - from: ".claude/CLAUDE.md"
      to: "CLAUDE.md"
      via: "design system sync"
      pattern: "#FF6D49"
---

<objective>
Refactor CLAUDE.md files and knowledge/integra-ecosystem.md to remove inline network data, fix design system contradictions, and add pointers to the new knowledge/networks/ files.

Purpose: CLAUDE.md currently inlines testnet-only config (chain ID 26218, deprecated RPC URL). .claude/CLAUDE.md has the old purple (#6C5CE7) + Inter font design system. integra-ecosystem.md has contradictory chain IDs. All three must be cleaned up so agents read from knowledge/networks/ instead.

Output: Refactored CLAUDE.md (root + .claude/), cleaned integra-ecosystem.md, fixed product-ideas.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md
@CLAUDE.md
@.claude/CLAUDE.md
@knowledge/integra-ecosystem.md
@knowledge/product-ideas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor root CLAUDE.md -- strip network data, add knowledge pointer</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (entire file -- this is the file being modified)
    - .planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md (lines 34-35 for CONFIG-01 scope, lines 115-123 for faucet fix)
  </read_first>
  <action>
Modify CLAUDE.md with these specific changes:

1. **Replace the Chain section (Section 1, lines 7-13)** with a network knowledge pointer:
```markdown
### Chain
- **Type:** EVM-compatible (Cosmos SDK + EVM module)
- **Networks:** Mainnet (Chain ID 26217) and Testnet (Chain ID 26218)
- **Token:** IRL / `airl` (18 decimals on EVM, 6 on Cosmos)

> **Network Configuration:** All chain IDs, RPC URLs, explorer URLs, gas prices, and endpoint details are in `knowledge/networks/`. Read `mainnet.md`, `testnet.md`, or `shared.md` as needed. **Never hardcode chain IDs or RPC URLs in agents, templates, or skills.**
```

2. **In the Infrastructure table (around line 26-32)**, update the Faucet row:
   - Change `faucet.integralayer.com -- 1000 IRL per address per 24h`
   - To: `faucet.integralayer.com -- 10 IRL + 1,000 tUSDI per request (24h cooldown)`

3. **In the Infrastructure table**, update Web3Auth row:
   - Remove `sapphire_devnet` hardcode
   - Change to: `sapphire_mainnet (mainnet) or sapphire_devnet (testnet) -- see knowledge/networks/`

4. **Remove the Explorer line** from the Chain section (it is now in knowledge/networks/ per-network files).

5. **In Section 8 Security Checklist**, update the Web3Auth line:
   - Change `Web3Auth configured for correct network (sapphire_devnet)`
   - To: `Web3Auth configured for correct network (sapphire_mainnet for mainnet, sapphire_devnet for testnet)`

Do NOT change Sections 2-7 or Section 9 (design system, code conventions, workflow, categories, integration, agent routing, skills). Those are correct and stay as-is.
  </action>
  <verify>
    <automated>grep -q "knowledge/networks/" CLAUDE.md && grep -q "10 IRL" CLAUDE.md && ! grep -q "testnet-rpc.integralayer.com" CLAUDE.md && ! grep -q "1000 IRL" CLAUDE.md && grep -q "26217" CLAUDE.md && grep -q "26218" CLAUDE.md && grep -q "Never hardcode" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "knowledge/networks/" pointer
    - CLAUDE.md contains "Never hardcode chain IDs or RPC URLs"
    - CLAUDE.md mentions both chain IDs (26217 mainnet, 26218 testnet) in summary form
    - CLAUDE.md faucet shows "10 IRL" not "1000 IRL"
    - CLAUDE.md does NOT contain "testnet-rpc.integralayer.com"
    - CLAUDE.md does NOT contain the old single-network Chain section with only chain ID 26218
    - CLAUDE.md still contains Section 2 Design System with "#FF6D49" and "Euclid Circular B"
    - CLAUDE.md security checklist mentions both sapphire_mainnet and sapphire_devnet
  </acceptance_criteria>
  <done>Root CLAUDE.md stripped of inline network data, points to knowledge/networks/, faucet corrected, Web3Auth mentions both networks</done>
</task>

<task type="auto">
  <name>Task 2: Sync .claude/CLAUDE.md -- fix design system, strip network data, match root</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (entire file -- this is the file being modified)
    - CLAUDE.md (the root file -- source of truth for design system after Task 1)
    - .planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md (lines 101-113 for design system contradictions)
  </read_first>
  <action>
The .claude/CLAUDE.md has a COMPLETELY WRONG design system (old purple palette, Inter font) and wrong faucet amount. This file must be updated to match the root CLAUDE.md.

Specific changes:

1. **Replace the entire Section 2 (Design System)** with the root CLAUDE.md's Section 2. This means:
   - Replace ALL colors: remove `#6C5CE7` (Brand Purple), `#A29BFE` (Brand Light), `#F0B90B` (Gold), `#00CEC9` (Teal), `#0A0A1A` (Background), `#12122A` (Card), `#0E0E22` (Section), `#2A2A4A` (Border), `#E8E8F0` (Text), `#8888AA` (Text Dim), `#00B894` (Success), `#FF6B6B` (Danger)
   - Replace with the correct Integra brand colors from root CLAUDE.md: Primary Coral `#FF6D49`, Primary Dark `#FC4E23`, Primary Light `#FF8A65`, Gold `#FFC17A`, Pink `#F34499`, Teal `#00A186`, Success `#1FC16B`, Warning `#FA7319`, Danger `#FA3748`, Info `#335CFF`
   - Add Dark Theme Surfaces: Background `#0A0A0F`, Card `rgba(23, 23, 28, 0.7)`, Border `rgba(255, 255, 255, 0.08)`, etc.
   - Replace Typography: remove Inter references entirely. Set to "Euclid Circular B (400/500/600/700)" for all UI, "Geist Mono" for code
   - Add Gradients section from root CLAUDE.md
   - Update Rules to match root (12px cards not 16px, 6px buttons not 10px, glassmorphism, etc.)

2. **Replace the Chain section** identically to how root CLAUDE.md was updated in Task 1 (network knowledge pointer, both chain IDs mentioned, "Never hardcode" directive).

3. **Fix faucet** in Infrastructure table: "10 IRL + 1,000 tUSDI per request (24h cooldown)" not "1000 IRL per address per 24h"

4. **Fix Security Checklist** Web3Auth line: mention both sapphire_mainnet and sapphire_devnet

The goal is that after this task, diffing Section 2 of .claude/CLAUDE.md and root CLAUDE.md shows zero meaningful differences in design system content.
  </action>
  <verify>
    <automated>! grep -q "#6C5CE7" .claude/CLAUDE.md && ! grep -q "#A29BFE" .claude/CLAUDE.md && ! grep -q "Inter" .claude/CLAUDE.md && grep -q "#FF6D49" .claude/CLAUDE.md && grep -q "Euclid Circular B" .claude/CLAUDE.md && grep -q "#0A0A0F" .claude/CLAUDE.md && grep -q "knowledge/networks/" .claude/CLAUDE.md && grep -q "10 IRL" .claude/CLAUDE.md && ! grep -q "1000 IRL" .claude/CLAUDE.md && ! grep -q "testnet-rpc.integralayer.com" .claude/CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/CLAUDE.md does NOT contain "#6C5CE7" (old purple)
    - .claude/CLAUDE.md does NOT contain "#A29BFE" (old purple light)
    - .claude/CLAUDE.md does NOT contain "Inter" as a font reference
    - .claude/CLAUDE.md contains "#FF6D49" (Coral primary)
    - .claude/CLAUDE.md contains "Euclid Circular B"
    - .claude/CLAUDE.md contains "#0A0A0F" (correct background)
    - .claude/CLAUDE.md contains Gradients section with "#F34499" and "#FF4F2E"
    - .claude/CLAUDE.md contains "knowledge/networks/" pointer
    - .claude/CLAUDE.md faucet shows "10 IRL" not "1000 IRL"
    - .claude/CLAUDE.md does NOT contain "testnet-rpc.integralayer.com"
    - .claude/CLAUDE.md mentions both sapphire_mainnet and sapphire_devnet
  </acceptance_criteria>
  <done>.claude/CLAUDE.md has correct Coral+Euclid design system matching root, knowledge/networks/ pointer, corrected faucet amount, no deprecated URLs</done>
</task>

<task type="auto">
  <name>Task 3: Clean integra-ecosystem.md and fix product-ideas.md faucet amount</name>
  <files>knowledge/integra-ecosystem.md, knowledge/product-ideas.md</files>
  <read_first>
    - knowledge/integra-ecosystem.md (entire file -- being modified)
    - knowledge/product-ideas.md (search for "1000 IRL" or "faucet" to find the line to fix)
    - .planning/phases/01-network-configuration-knowledge-refactor/01-RESEARCH.md (lines 36-37 for CONFIG-03 scope)
  </read_first>
  <action>
**For knowledge/integra-ecosystem.md:**

1. Read the file to identify the chain config section. Per research, lines 1-57 contain a chain configuration table, a hardhat config code block, and a wagmi chain code block. These all duplicate (and contradict) the new knowledge/networks/ files.

2. Remove the chain configuration table, the hardhat config code block, and the wagmi chain code block from the top of the file.

3. Replace the removed content with a brief pointer:
```markdown
## Network Configuration

> Network config (chain IDs, RPCs, explorers, gas) is in `knowledge/networks/`. See `mainnet.md`, `testnet.md`, and `shared.md`. Do not duplicate network data here.
```

4. Keep everything else in the file (products, APIs, contract descriptions, integration guides).

5. If there is a Web3Auth config code section (research says lines 857-889), check it for the old `#6C5CE7` theme color. If found, change to `#FF6D49`. Check for `testnet-rpc.integralayer.com` and replace with a pointer to `knowledge/networks/` or remove the hardcoded URL.

**For knowledge/product-ideas.md:**

1. Search for "1000 IRL" and replace with "10 IRL + 1,000 tUSDI" (corrected faucet amount per NET-06).
  </action>
  <verify>
    <automated>grep -q "knowledge/networks/" knowledge/integra-ecosystem.md && ! grep -q "testnet-rpc.integralayer.com" knowledge/integra-ecosystem.md && ! grep -q "1000 IRL" knowledge/product-ideas.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - knowledge/integra-ecosystem.md contains "knowledge/networks/" pointer at the top
    - knowledge/integra-ecosystem.md does NOT contain inline chain ID tables or hardhat/wagmi code blocks with chain config
    - knowledge/integra-ecosystem.md does NOT contain "testnet-rpc.integralayer.com"
    - knowledge/integra-ecosystem.md does NOT contain "#6C5CE7" if Web3Auth section existed
    - knowledge/product-ideas.md does NOT contain "1000 IRL" (old faucet amount)
    - knowledge/product-ideas.md contains "10 IRL" (corrected amount)
    - Both files still contain their non-network content (products, APIs, ideas)
  </acceptance_criteria>
  <done>integra-ecosystem.md stripped of network config with knowledge pointer added, product-ideas.md faucet amount corrected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md to agent behavior | Agents follow CLAUDE.md instructions; wrong data here propagates to all generated code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | CLAUDE.md network pointer | accept | Pointer directs to public chain config only; no secrets. Knowledge files contain only public blockchain endpoints. |
| T-01-04 | Tampering | .claude/CLAUDE.md design system | mitigate | Verify grep for old values (#6C5CE7, Inter) returns zero hits after update. Automated acceptance criteria enforce this. |
</threat_model>

<verification>
```bash
# Root CLAUDE.md has no deprecated values
! grep -q "testnet-rpc.integralayer.com" CLAUDE.md && ! grep -q "1000 IRL" CLAUDE.md

# .claude/CLAUDE.md has no old design system
! grep -q "#6C5CE7" .claude/CLAUDE.md && ! grep -q "Inter" .claude/CLAUDE.md

# integra-ecosystem.md has no inline chain config
! grep -q "testnet-rpc.integralayer.com" knowledge/integra-ecosystem.md

# product-ideas.md faucet fixed
! grep -q "1000 IRL" knowledge/product-ideas.md
```
</verification>

<success_criteria>
- Root CLAUDE.md lean: rules and routing only, network data replaced by knowledge/networks/ pointer
- .claude/CLAUDE.md design system identical to root (Coral #FF6D49, Euclid Circular B)
- integra-ecosystem.md has zero network configuration, only products/APIs
- All four files have zero references to deprecated testnet-rpc URL
- All four files have correct faucet amount (10 IRL + 1,000 tUSDI)
</success_criteria>

<output>
After completion, create `.planning/phases/01-network-configuration-knowledge-refactor/01-02-SUMMARY.md`
</output>
